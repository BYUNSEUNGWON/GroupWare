<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bsw.groupware.mapper.MenuMapper">
    
    <select id="getList">
    	SELECT 
    		MENU_ID,
    		MENU_NM,
    		MENU_SEQ,
    		MENU_URL
   		 FROM 
    	 	IA_MENU_TN
    </select>
    
</mapper>